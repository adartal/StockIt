# shared-types

TypeScript types generated from the API's pydantic models (via `datamodel-code-generator`).

Populated in M1 once the core schemas land. Until then this package is intentionally empty.
